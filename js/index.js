//regresando  a posicion inicial el seccion de proyectos
const header = document
  .querySelector("header")
  .addEventListener("mouseover", () => {
    const checkbox = (document.getElementById("checkProyects").checked = false);
  });
